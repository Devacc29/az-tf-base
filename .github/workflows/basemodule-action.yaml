name: Base-Module-tf-action
on:
  pull_request:
    types:
        - closed
        - opened
    branches:
        - main
  push:
    branches:
      - main
    paths-ignore:
      - '.github/workflows/*'
  workflow_dispatch:

jobs:
    ResourceGroup:
      uses: ./.github/workflows/Terraform.yaml